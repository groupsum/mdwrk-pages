schema_version: "0.6.0"
kind: "spec"
id: "spc:2083"
number: 2083
slug: "portable-structured-data-reusable-component-contract"
title: "Portable Structured Data Reusable Component Contract"
status: "draft"
origin: "repo-local"
decision_date: null
tags: []
summary: "This SPEC governs the reusable component obligations for every supported JSON-LD structured-data type across `@mdwrk/structured-data`, `@mdwrk/lander-react`, `@mdwrk/lander-core`, `@mdwrk/lander-content-contract`, `@mdwrk/mdwrkcom-content-pack`, and `apps/mdwrkcom`."
supersedes: []
superseded_by: []
status_notes:
  -
    at: "2026-05-13T05:42:28Z"
    note: "Created to govern per-type reusable component ownership and adoption for structured-data surfaces."
    status: "draft"
  -
    at: "2026-05-13T09:08:46Z"
    note: "Updated for content-pack-driven schema intent compilation and lander-react rendering."
    status: "draft"
  -
    at: "2026-05-13T09:13:42Z"
    note: "Preserved existing reusable component obligations and appended content-pack-driven schema intent flow."
    status: "draft"
  -
    status: "draft"
    note: "Added explicit breadcrumb split between structured-data builders, core route semantics, and lander-react UI."
    at: "2026-05-21T04:35:52Z"
references: []
body: |-
  # SPEC-2083: Portable Structured Data Reusable Component Contract
  
  ## Scope
  
  This SPEC governs the reusable component obligations for every supported JSON-LD structured-data type across `@mdwrk/structured-data`, `@mdwrk/lander-react`, `@mdwrk/lander-core`, `@mdwrk/lander-content-contract`, `@mdwrk/mdwrkcom-content-pack`, and `apps/mdwrkcom`.
  
  ## Requirements
  
  1. Each governed supported structured-data type shall have an explicit implementation state tracked across three layers: builder support in `@mdwrk/structured-data`, reusable component or composition support in `@mdwrk/lander-react`, and mdwrkcom adoption.
  2. `@mdwrk/structured-data` shall own typed builders, schema helpers, and graph primitives for supported Schema.org JSON-LD types.
  3. `@mdwrk/lander-react` shall own the reusable concrete component or composition surface corresponding to each governed supported structured-data type.
  4. `apps/mdwrkcom` shall import governed structured-data components or composition helpers from `@mdwrk/lander-react` when mdwrkcom uses that type as part of public lander behavior.
  5. `apps/mdwrkcom` shall not define app-local long-term ownership for governed supported structured-data type components when an equivalent lander-owned reusable surface is required.
  6. Supported structured-data types with visible or materially represented content shall expose a lander-owned reusable component surface that preserves visible-content parity with the structured-data claims.
  7. Supported structured-data types that are non-visual or primarily graph-compositional shall expose a lander-owned reusable composition helper or equivalent governed adoption surface.
  8. Export naming, prop shape, and styling of lander-owned structured-data components shall follow governed component class and token policy rather than ad hoc app-local utility composition.
  9. New supported structured-data types are not implementation-complete until the builder layer, reusable lander layer, and adoption or explicit non-adoption state are recorded and linked.
  10. Verification shall fail closed when mdwrkcom emits or visibly represents a governed structured-data type through app-local one-off implementations that bypass the lander-owned reusable surface.
  11. Verification shall fail closed when a type is marked supported but lacks the required builder support or required lander-owned reusable component or composition support.
  12. Browser screenshot validation shall include affected visible structured-data component surfaces for each shipped lander theme when a change alters those visible surfaces.
  13. Every supported JSON-LD structured-data type shall have a typed builder or graph primitive in `@mdwrk/structured-data`.
  14. Every supported JSON-LD structured-data type shall have a reusable React component or component adapter in `@mdwrk/lander-react`.
  15. Reusable structured-data React components shall emit deterministic JSON-LD output through the structured-data package rather than maintaining independent schema serialization logic.
  16. Structured-data component selection shall originate from content-pack schema declarations that conform to `@mdwrk/lander-content-contract` and are compiled by `@mdwrk/lander-core`.
  17. `@mdwrk/lander-core` shall normalize schema intent into a portable render model without importing React.
  18. `@mdwrk/lander-react` shall map compiled structured-data intent to JSON-LD graph output and the corresponding type-specific reusable component surface.
  19. Breadcrumbs are explicit: `@mdwrk/structured-data` owns `BreadcrumbList` builders, `@mdwrk/lander-react` owns visible breadcrumb rendering, and `@mdwrk/lander-core` only supplies route hierarchy semantics.
  20. `apps/mdwrkcom` shall consume structured-data support through the content-pack, core compiler, and lander-react renderer flow instead of choosing per-type structured-data React components in page-local app code.
  21. The content contract shall reject malformed schema intent, unknown schema kinds, and app-local implementation references.
  22. Tests shall cover structured-data builder output, content-contract validation, lander-core schema intent compilation, lander-react render output, and mdwrkcom host adoption or explicit non-adoption for each supported type.
  
  ## Acceptance
  
  The structured-data and lander-react boundaries shall expose a matrix with one row per supported JSON-LD type. Each row shall show structured-data implementation, lander-react reusable component implementation, content-pack declarability, core compilation, mdwrkcom usage or non-adoption, feature IDs, claim IDs, test IDs, and current evidence status.
spec_kind: "normative"
adr_ids:
  - "adr:1049"
  - "adr:1051"
  - "adr:1057"
  - "adr:1062"
  - "adr:1063"
