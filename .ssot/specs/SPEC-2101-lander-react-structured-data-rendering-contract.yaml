schema_version: "0.7.0"
kind: "spec"
id: "spc:2101"
number: 2101
slug: "lander-react-structured-data-rendering-contract"
title: "Lander React Structured Data Rendering Contract"
status: "draft"
origin: "repo-local"
decision_date: null
tags: []
summary: "This SPEC governs the visible and wrapper rendering contract for generated structured-data artifacts in `@mdwrk/lander-react-structured-data` and `@mdwrk/lander-react`."
supersedes: []
superseded_by: []
status_notes:
  -
    at: "2026-05-23T00:00:00Z"
    note: "Created to govern the new lander-react-structured-data package boundary and migration contract."
    status: "draft"
  -
    status: "draft"
    note: "Expanded rendering contract to full-field generated surfaces and token-backed fused coverage."
    at: "2026-06-01T09:09:29Z"
  -
    status: "draft"
    note: "Added rendering contract requirements for page-level graph collection and local emission fallback parity."
    at: "2026-06-07T00:00:00Z"
references: []
body: |-
  # SPEC-2101: Lander React Structured Data Rendering Contract
  
  ## Scope
  
  This SPEC governs the visible and wrapper rendering contract for generated structured-data artifacts in `@mdwrk/lander-react-structured-data` and `@mdwrk/lander-react`.
  
  ## Requirements
  
  1. Generated `types`, `properties`, `enumerations`, and `datatypes` shall expose concrete wrapper and fused rendering surfaces when promoted.
  2. Generated surfaces shall accept direct top-level schema-backed fields rather than forcing an opaque nested payload wrapper as the only authored path.
  3. Visible fused surfaces shall render all present fields recursively in a deterministic way unless a governed fail-closed exception applies.
  4. All fused generated surfaces shall support `className`.
  5. Fused token ownership shall exist for every promoted generated artifact.
  6. Demo and fixture surfaces shall expose representative generated artifacts from each artifact kind.
  7. Visible fused semantic components shall expose `.toStructuredData(props)` when they are promoted for structured-data page composition.
  8. Page-level collection mode shall suppress component-local JSON-LD scripts and emit a single page-owned `@graph` script assembled from collected component contributions.
  9. Standalone rendering outside collection mode shall preserve component-local JSON-LD emission using the same structured-data node construction path as collection mode.
  10. Collected graphs shall preserve stable identifiers and relationships supplied by the component contribution helpers.
  
  ## Acceptance
  
  Linked tests shall prove wrapper completeness, fused rendering completeness, className coverage, token coverage, graph collection behavior, and standalone emission parity for promoted generated artifacts.
spec_kind: "normative"
adr_ids:
  - "adr:1057"
  - "adr:1062"
  - "adr:1063"
  - "adr:1065"
  - "adr:1079"
  - "adr:1084"
  - "adr:1085"
  - "adr:1086"
