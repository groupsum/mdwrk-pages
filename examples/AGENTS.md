# MdWrk Pages Example Agents

This folder owns downstream example applications for MdWrk Pages packages.

- Keep examples focused on consuming published workspace packages through package imports.
- Do not place reusable Pages runtime behavior in examples; move it to the owning package under `packages/`.
- Example builds must remain runnable from the repository root through the root package scripts.
