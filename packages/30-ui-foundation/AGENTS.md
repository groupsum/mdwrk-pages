# MdWrk Pages UI Foundation Package Agents

This layer owns tokens, theme assets, and reusable primitive UI building blocks.

- Keep styling surfaces and primitives reusable by renderers and demos.
- Do not move page compilation, page-template graph semantics, or JSON-LD output into this layer.
- UI foundation changes must preserve public style exports, token coverage, and primitive package tests.
