# MdWrk Pages Shared Package Agents

This folder owns shared packages consumed by the Pages suite.

- Keep structured-data builders reusable across Pages rendering and discovery outputs.
- Do not place app host, mdwrk.com host, or one-off example behavior in shared packages.
- Shared package claims require package tests that exercise public exports.
