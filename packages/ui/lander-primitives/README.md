# @mdwrk/lander-primitives

Shared React UI primitives for MdWrk Pages semantic components and demos.

The package owns reusable UI behavior and markup. Domain-specific semantic
components should compose these primitives rather than duplicating local card,
toolbar, form, navigation, media, and preview structures.
