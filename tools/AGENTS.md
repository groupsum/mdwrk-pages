# MdWrk Pages Tools Agents

This folder owns MdWrk Pages automation.

- Keep package-suite validators, governance checks, and workspace helpers here.
- Tool scripts must run from the repository root and use repo-relative paths.
- Governance tests must fail closed when required CR rows, SSOT links, execution bodies, package scripts, or scoped agent files are missing.
